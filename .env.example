# Database
POSTGRES_USER=your_db_user
POSTGRES_PASSWORD=your_db_password
POSTGRES_DB=arepabuelas

# MinIO
MINIO_ROOT_USER=your_minio_user
MINIO_ROOT_PASSWORD=your_minio_password
MINIO_ACCESS_KEY=your_minio_access_key
MINIO_SECRET_KEY=your_minio_secret_key
MINIO_BUCKET=user-photos

# Backend
NODE_ENV=development
PORT=4000
DATABASE_URL=postgres://your_db_user:your_db_password@db:5432/arepabuelas
MINIO_ENDPOINT=minio
MINIO_PORT=9000

# JWT - Generate a secure random string for production
JWT_SECRET=your_jwt_secret_key_here
ACCESS_EXPIRES=15m
REFRESH_TTL_DAYS=30

# Encryption - Generate with: node -e "console.log(require('crypto').randomBytes(32).toString('base64'))"
ENCRYPTION_KEY=your_encryption_key_here

# Frontend
VITE_API_URL=http://localhost:4000
